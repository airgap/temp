import type { TtfMatch } from '@lyku/json-models';
import type { Player } from '@lyku/helpers';
export declare const placePieceInMatch: (
	iAm: Player,
	square: number,
	match: TtfMatch,
) => TtfMatch;
